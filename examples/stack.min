; program that computes the equation:
; (1 + 2) * 3
; using reverse-polish notation on the stack

.text
psh 1
psh 2

pop ; pop 2
add ^ ; add 2 to 1 (1 is on top of the stack)
drp ; drop 1 from the stack
psh ; push result = 3

psh 3
pop ; pop 3
mul ^ ; multiple 3 by result 3 on top of the stack
drp ; drop 3 from the stack
psh ; push the result
pop x ; pop the total result into x

pri ; print the result
prc '\n'
hlt

.data
x dat 0
temp dat 0